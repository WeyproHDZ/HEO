extends layouts/default.pug

block value
  - var currentPage = 'login';

block main
  section.banner-container(style="background-image: url('images/banner_start.png')")
    .block_container
      .restrict.allpadding
        h1 歡迎登入使用
        
      .login
        .block_inner
          .login_items
            .item
              h2 會員登入
              form.login_form
                .controls_group
                  label 帳號
                  //↓controls class 加入 "error" 可顯示錯誤提示↓
                  .controls
                    input(type="email", placeholder="請輸入FB帳號")
                    .error_text *請輸入Facebook帳號
                .controls_group
                  label 密碼
                  //↓controls class 加入 "error" 可顯示錯誤提示↓
                  .controls
                    input(type="password", placeholder="請輸入FB密碼")
                    .error_text *請輸入密碼
                .captcha
                  .captcha_inner
                    .g-recaptcha(data-sitekey="6LcHGhITAAAAABIgEAplK2EWsVFkaE5o0DWUpsIs")  
                //- .row.captcha-new-wrapper
                //-   .controls_group
                //-     label 驗證碼
                //-   .grid.g_6_12
                //-     //↓controls class 加入 "error" 可顯示錯誤提示↓
                //-     .controls
                //-       input(type="text", placeholder="驗證碼")
                //-       .error_text *請輸入驗證碼
                //-   .grid.g_6_12
                //-     .controls
                //-       .captcha-frame
                     
                .controls_group.terms-group
                  input#terms(type="checkbox", name="terms")
                  label(for="terms") 我同意HeO臉書互惠系統
                    span 
                      a.terms.ajax-popup-link(href="terms.html") 使用條款
                a.btn(href="certified.html" title="登入") 登入
            .instruction
              .content-block
                .about-content
                  h3 使用說明
                  ul
                    li
                      p 登入後即同意HeO互惠平台相關規定,並接受該臉書在登入期間會自動幫有需要之會員作業服務。
                        
                    li
                      p HeO目前僅提供臉書互惠,請先使用臉書登入HEO,登入後會員可在使用頁面輸入您要作業的網址,並填入數量送出後,其他會員就會在登入HEO平台期間為您的輸入的網址進行作業,在您登入期間若有人像您帳號購買您的服務,則會額外得到獎勵金。
                    li
                      p 初次登入需要檢查該臉書基本門檻,認證過下次無需在認證,初步認證僅需簡單帳號檢測,若登入的臉書帳號是真人在使用,或極為像真人,每組帳號可以像工作人員申請真人帳號資格,真人帳號可獲取更多免費時間與更多獎勵金。
                    li
                      p 如若有操作上的問題可參考
                        span
                          a.font-purple(href="support.html", title="使用教學") 使用教學。


block JS
  script(src='https://www.google.com/recaptcha/api.js')